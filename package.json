{
  "name": "tc55-sockets-api",
  "private": true,
  "scripts": {
    "build": "mkdir -p out && curl --retry 2 --fail -L https://www.w3.org/publications/spec-generator/ --output out/index.html -F type=bikeshed-spec -F die-on=fatal -F file=@index.bs"
  }
}
